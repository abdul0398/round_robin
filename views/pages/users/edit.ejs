<!DOCTYPE html>
<html lang="en">
<%- include('../../partials/head', { title: 'Edit Participant - JJ Leads RR System' }) %>
<body>
    <div class="container">
        <%- include('../../partials/header') %>
        <%- include('../../partials/navigation', { activeSection: 'users' }) %>

        <div class="section active">
            <h2>Edit Participant: <%= participant.name %></h2>
            
            <% if (typeof error !== 'undefined' && error) { %>
                <div class="alert alert-danger">
                    <%= error %>
                </div>
            <% } %>
            
            <form action="/users/<%= participant.id %>/edit" method="POST">
                <div class="form-group">
                    <label for="name">Full Name</label>
                    <input type="text" id="name" name="name" value="<%= participant.name %>" placeholder="Enter participant's full name" required>
                </div>

                <div class="form-group">
                    <label for="discordName">Discord Name</label>
                    <input type="text" id="discordName" name="discordName" value="<%= participant.discord_name %>" placeholder="e.g., username#1234" required>
                </div>

                <div class="form-group">
                    <label for="discordWebhook">Discord Webhook URL</label>
                    <input type="url" id="discordWebhook" name="discordWebhook" value="<%= participant.discord_webhook %>" placeholder="https://discord.com/api/webhooks/..." required>
                    <small class="form-text">This is where leads will be sent for this participant</small>
                </div>


                <div class="form-actions">
                    <button type="submit" class="btn-primary">Update Participant</button>
                    <a href="/users/<%= participant.id %>" class="btn-secondary">Cancel</a>
                </div>
            </form>
            
            <div class="participant-info">
                <div class="info-card">
                    <h3>Participant Information</h3>
                    <div class="info-row">
                        <label>Status:</label>
                        <span class="status-badge <%= participant.is_active ? 'active' : 'inactive' %>">
                            <%= participant.is_active ? 'Active' : 'Inactive' %>
                        </span>
                    </div>
                    <div class="info-row">
                        <label>Created:</label>
                        <span><%= new Date(participant.created_at).toLocaleDateString() %></span>
                    </div>
                    <% if (participant.updated_at && participant.updated_at !== participant.created_at) { %>
                    <div class="info-row">
                        <label>Last Updated:</label>
                        <span><%= new Date(participant.updated_at).toLocaleDateString() %></span>
                    </div>
                    <% } %>
                </div>
            </div>
        </div>
    </div>

    <style>
        .form-actions {
            display: flex;
            gap: 10px;
            margin-top: 30px;
            padding-top: 20px;
            border-top: 1px solid #dee2e6;
        }
        
        .participant-info {
            margin-top: 30px;
        }
        
        .info-card {
            background: #ffffff;
            border: 1px solid #dee2e6;
            border-radius: 8px;
            padding: 20px;
        }
        
        .info-card h3 {
            margin: 0 0 15px 0;
            color: #002655;
            border-bottom: 1px solid #dee2e6;
            padding-bottom: 10px;
        }
        
        .info-row {
            display: flex;
            margin-bottom: 10px;
            padding: 5px 0;
        }
        
        .info-row label {
            font-weight: 600;
            min-width: 120px;
            color: #495057;
        }
        
        .info-row span {
            color: #212529;
        }
        
        .status-badge {
            padding: 4px 8px;
            border-radius: 4px;
            font-size: 0.875rem;
            font-weight: 500;
        }
        
        .status-badge.active {
            background: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }
        
        .status-badge.inactive {
            background: #f8d7da;
            color: #721c24;
            border: 1px solid #f1b0b7;
        }
        
        .form-text {
            display: block;
            margin-top: 5px;
            font-size: 0.875rem;
            color: #6c757d;
        }
        
        .alert {
            padding: 12px 15px;
            border: 1px solid transparent;
            border-radius: 4px;
            margin-bottom: 20px;
        }
        
        .alert-danger {
            color: #721c24;
            background-color: #f8d7da;
            border-color: #f1b0b7;
        }
    </style>
</body>
</html>